build:
  docker:
    web: Dockerfile

run:
  web:
    command:
      - JUDGE_PROCESS_MODE=web npm run start -- --hostname 0.0.0.0 --port $PORT
    image: web
  worker:
    command:
      - JUDGE_PROCESS_MODE=worker npm run worker:judge
    image: web
